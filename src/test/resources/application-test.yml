kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}

kafkademo:
    thirdpartyEndpoint: http://localhost:${wiremock.server.port}/api/kafkawithdynamodbdemo/

amazon:
    access:
        key: key
        secret-key: secret-key
    region: eu-west-2
    end-point:
        url: http://localhost:8000/
